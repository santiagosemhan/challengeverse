{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Challengeverse API",
            "program": "${workspaceFolder}/challengeverse-api/server.js"
        }
    ]
}